# 密钥配置更新说明

## 📝 更新内容

根据用户反馈，已还原默认 API 密钥配置，并优化了错误处理逻辑。

---

## ✅ 已完成的修改

### 1. **还原默认密钥**

#### `public/preload/services.js`
- 添加回默认密钥：`DEFAULT_API_KEY = '5e690ed1f4cbdb16d88a3f6a07e9f185'`
- `getApiKey()` 函数优先使用用户配置的密钥，如果没有则返回默认密钥
- 移除了"请先配置密钥"的错误检查

#### `src/dev-mock.js`
- 添加回相同的默认密钥
- 翻译函数优先使用用户配置的密钥，如果没有则使用默认密钥

### 2. **优化设置页面 UI**

#### 标签更新
```
旧: API-KEY * (必填)
新: API-KEY (可选，留空使用默认密钥)
```

#### 占位符更新
```
旧: 请输入您的 API-KEY
新: 留空使用默认共享密钥，或输入您自己的 API-KEY
```

#### 提示文案更新
```
旧: 请填写您自己的 API 密钥以使用翻译功能
新: 使用默认共享密钥即可正常翻译，配置自己的密钥可获得更好的服务稳定性
```

#### 按钮文本更新
```
旧: 清除配置（使用默认）
新: 恢复默认密钥
```

#### 测试按钮
- 移除 `:disabled="!apiKey.trim()"` 限制
- 现在可以使用默认密钥测试连接

### 3. **错误处理优化**

现在的行为：
1. ✅ 首次使用自动使用默认密钥
2. ✅ API 调用失败不会阻止手动输入翻译
3. ✅ 用户可以随时手动输入翻译并保存单词
4. ✅ 默认密钥不足时，提示用户配置自己的密钥

---

## 🔄 用户体验流程

### 场景 1：新用户（推荐流程）

```
1. 打开翻译功能
2. 输入单词或中文
3. 自动使用默认密钥翻译
4. 保存单词到单词本
5. 正常使用 ✅
```

### 场景 2：API 失败或限流

```
1. 翻译时显示错误：翻译失败（可能是密钥限流）
2. 用户点击"✏️ 手动输入翻译"
3. 输入翻译内容
4. 保存单词（添加"手动输入"标签）
5. 正常使用 ✅
```

### 场景 3：配置自己的密钥

```
1. 进入"设置"页面
2. 在 API-KEY 输入框填入自己的密钥
3. 点击"💾 保存配置"
4. 后续翻译使用自己的密钥
5. 获得更稳定的服务 ✅
```

---

## 📋 核心代码逻辑

### 密钥获取逻辑

```javascript
function getApiKey() {
  try {
    // 1. 尝试获取用户配置的密钥
    if (window.utools?.db) {
      const doc = window.utools.db.get(STORAGE_KEYS.API_KEY)
      if (doc?.data) return doc.data  // 返回用户配置的密钥
    }
  } catch (e) {
    console.error('获取API密钥失败:', e)
  }
  // 2. 返回默认密钥（降级方案）
  return DEFAULT_API_KEY
}
```

### 翻译错误处理

```javascript
// Translate 组件中的错误处理
if (result.success) {
  // 翻译成功，显示结果
  translation.value = result.translation
} else {
  // 翻译失败，显示错误和手动输入选项
  error.value = result.error || '翻译失败'
  // 用户仍可手动输入翻译 ✅
}
```

---

## 🎯 设计理念

### 渐进式增强

1. **开箱即用**: 默认密钥让用户可以立即使用
2. **平滑降级**: API 失败时仍可手动输入
3. **用户选择**: 可随时配置自己的密钥获得更好服务

### 错误容忍

- ✅ API 失败不影响核心功能
- ✅ 手动输入的翻译可以正常保存
- ✅ 所有功能在无 API 密钥时仍可用

---

## 🚀 使用建议

### 对于普通用户
- 直接使用即可，默认密钥满足日常需求
- 遇到翻译错误时，使用手动输入功能

### 对于高级用户
- 建议配置自己的 API 密钥
- 可获得更高的调用限额和稳定性
- 免费额度：新用户 100 万字符

### 如何获取 API 密钥
1. 访问 https://niutrans.com/cloud/account/register
2. 注册账号
3. 登录后进入「个人中心」→「我的密钥」
4. 复制 API-KEY
5. 在设置页面填入并保存

---

## 📊 技术细节

### 密钥优先级
```
用户配置密钥 > 默认共享密钥 > 无密钥（错误）
```

### 错误场景处理
| 场景 | 行为 |
|------|------|
| 首次使用 | 使用默认密钥 ✅ |
| 用户配置密钥 | 使用用户密钥 ✅ |
| API 调用失败 | 显示错误 + 手动输入选项 ✅ |
| 用户手动输入 | 正常保存单词 ✅ |
| 密钥限流 | 提示配置自己的密钥 💡 |

---

## ✅ 总结

现在的配置策略：
1. **默认密钥始终可用** - 用户开箱即用
2. **手动输入始终可用** - API 失败时也能正常使用
3. **用户可配置自己的密钥** - 获得更好的服务体验
4. **错误提示友好** - 引导用户解决问题

这符合互联网产品的最佳实践：**提供合理的默认值，同时允许用户自定义**。
